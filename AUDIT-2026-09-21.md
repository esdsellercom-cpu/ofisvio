# OFISVIO — Final Production Audit (21 Eylül 2026) → FIX → RE-AUDIT (22 Eylül 2026)

Denetim commit'i: `8f8c15f` (kod değiştirilmeden). Düzeltmeler: `5c627fd` (FIX 1/3), `af8b6d7` (FIX 2/3), FIX 3/3 (bu commit).
Yöntem: kaynak taraması, mimari testlerin kapsamı, yerel yeniden üretim; doğrulanamayan kalem **UNKNOWN**. "Muhtemelen çalışıyor" yok.

## 0. Mimari sapma

Görev metni Inertia + Vue 3 + TypeScript + MySQL + Install Wizard varsayar. Gerçek yığın: **Laravel 13 / PHP 8.3, Blade +
bağımlılıksız JS**, geliştirme/CI SQLite, üretim MySQL/PgSQL (DEPLOY.md). JSON API / TS sözleşmesi **N/A**; parity denetimi
rota ↔ form ↔ FormRequest ↔ servis ↔ DB düzeyinde. Kurulum web sihirbazı yerine **`ofisvio:install` CLI** (bilinçli: dışa açık
yüzey yok). Bu sapmalar ürün kararıdır.

## A. Executive summary

| | Denetim (21 Eylül) | Re-audit (22 Eylül) |
|---|---|---|
| Production Readiness | **BLOCKED** (P0 ×5, P0B ×5) | **CONDITIONAL** — kod tarafı kapalı; kalan koşullar §P |
| Testler | 362/362 | **370/370** (pint ✓ · phpstan L6 0 ✓ · build ✓ · CI ✓) |

## B. Bulgular ve durumları

| ID | Sev | Bulgu | Düzeltme | Kanıt (test) | Durum |
|---|---|---|---|---|---|
| F-01 | P0 | `recordPayment` bakiye kontrolü işlem dışı, kilit yok, referans tekil değil → çift/fazla tahsilat | `lockForUpdate` ile güncel satırda kontrol; aynı referans reddi; 60 sn çift tık penceresi; çağıran modeli kilitli satırla eşitlenir | `CollectionCenterTest::tahsilat_kaydi_kilitli_satirda…` | ✅ |
| F-02 | P0 | Artefakt/imza/provenance/deploy/post-deploy zinciri yok | CI `release` job (`--no-dev` paket, `RELEASE.json`, sha256, SLSA attestation); `deploy/deploy.sh` (sha256 → `gh attestation verify` → down → migrate → doctor → current → smoke → up; düşerse bakımda kalır; `--rollback`); `ofisvio:smoke` (14 gerçek kontrol) | `SmokeCommandTest`; deploy.sh `bash -n` | ✅ kod/CI · sunucu provası **UNKNOWN** (gerçek sunucu yok) |
| F-03 | P0 | Backup/restore yok | `BackupService` + `ofisvio:backup` (günlük; DB dökümü + private + medya; sha256; AES-256-GCM parça şifreleme; retention) + `ofisvio:restore --force` (bakım → DB + dosya → doctor kapısı); doctor yedek yaşı | `BackupRestoreTest` (BACKUP → DESTROY → RESTORE → VERIFY, sqlite) | ✅ · mysqldump/pg_dump yolu **UNKNOWN** (yerelde MySQL yok) |
| F-04 | P0 | NTP/saat denetimi yok | doctor: DB ↔ uygulama saat farkı (> 5 sn hata), Linux'ta `timedatectl`/`chronyc` senkron durumu | `DoctorCommandTest` (satır basılır) | ✅ (Linux sunucuda **UNKNOWN**) |
| F-05 | P0 | Zamanlayıcı `onOneServer` yok → çok sunucuda tekrar | tüm görevler `withoutOverlapping()->onOneServer()` | `SchedulerIsolationTest` | ✅ |
| F-06 | P0B | GA4 rıza olmadan yükleniyor, çerez bandı yok | `CookieConsent` sunucu çerezi, `POST /cerez-tercihi`, GA4/GTM yalnız `all`; bant metni footer ayarında | `SeoAdvancedTest` | ✅ |
| F-07 | P0B | Rıza hangi metne verildi kanıtı yok | `legal_document_versions` (append-only; footer seçimi + içerik yayını gövde özeti değişince yeni sürüm) + `consent_records` (lead/booking/franchise/etkinlik); doctor üretimde KVKK sürümü şart | `SiteChromeTest`, `DoctorCommandTest` | ✅ |
| F-08 | P0B | TC kimlik düz metin; audit PII maskelemiyor | `identity_number` `encrypted` cast + yerinde şifreleme migration'ı; audit maskesi `identity_number/iban/card_number` | `MemberCenterTest` | ✅ |
| F-09 | P0B | Saklama/imha politikası yok | `RetentionService` + `ofisvio:retention` (günlük) + `privacy.*` ayarları; anonimleştirme satır+rıza izini korur; eski KYC dosyası yalnız ayar açıkken | `RetentionTest` | ✅ |
| F-10 | P0B | MASAK yükümlülüğü değerlendirilmemiş | — (hukuki karar) | — | ⏳ **açık: hukuk görüşü** |
| F-11 | P1 | CSP `script-src 'unsafe-inline'` | istek başına nonce + `strict-dynamic`; 63 satır içi olay özniteliği `data-*` ile JS'e taşındı; kaynak taraması testte | `SecurityHeadersTest` | ✅ |
| F-12 | P1 | TrustProxies yok | `TRUSTED_PROXIES` (config) → `App\Http\Middleware\TrustProxies`; doctor uyarısı | `SecurityHeadersTest` | ✅ |
| F-13 | P1 | iyzico/e-Fatura adaptörsüz ama yapılandırılabiliyordu | `kind: planned` — yapılandırma/test kabul edilmez, "Planlı (adaptör yok)" | `IntegrationHubTest` | ✅ (modül hâlâ yok — dürüst durum) |
| F-14 | P1 | Anahtar rotasyonu yok | `KeyRotationService` + `ofisvio:reencrypt` (`APP_PREVIOUS_KEYS`), doctor bekleyen kayıt | `KeyRotationTest` (gerçek anahtar değişimi) | ✅ |
| F-15 | P1 | Ledger yok | `ledger_entries` append-only (model update/delete reddeder), `InvoiceService::ledger`, `/panel/tahsilat/defter` (`ledger.view`) | `CollectionCenterTest` | ✅ (düzeltme kaydı UI'ı: `ledger.correction_entry` planlı) |
| F-16 | P1 | CI'da bağımlılık denetimi yok | `composer validate --strict`, `composer audit`, `npm audit --audit-level=high` | CI run 82 ✓ | ✅ |
| F-17 | P1 | Install wizard yok | `ofisvio:install` (`--check`, `--upgrade`; kilit dosyası; doctor) | `SmokeCommandTest` (`--check`) | ✅ |
| F-18 | P1 | Alarm yok | `ofisvio:health-alert` (15 dk): doctor hatası / failed_jobs → `system.alert` (in-app + e-posta, 6 sa tekrar bastırma); doctor "Başarısız işler" | `HealthAlertTest` | ✅ |
| F-19 | P1 | Kılık değiştirmiş yükleme testleri eksik | PHP-as-JPG, çift uzantı, SVG-XSS, HTML, JS, EXE, yol geçişi vakaları; **bulgu:** küçük görseller yeniden kodlanmıyordu (polyglot PNG+PHP diske geçiyordu) → her görsel GD ile yeniden kodlanır, GD yok/çözülemez → ret | `MediaLibraryTest` | ✅ |
| F-20 | P1 | Favicon yok | Header ayarında favicon / apple-touch-icon | `SiteChromeTest` | ✅ |
| F-21..26 | P2 | ₺ etiketleri, hreflang tek dil, planlı izinler, offboarding komutu, tema renk/tipografi | — | — | ⏳ yol haritası |
| **Yeni** | P0 | **Smoke buldu:** gelen webhook CSRF istisnası (`withoutMiddleware(ValidateCsrfToken)`) Laravel 13'ün `PreventRequestForgery`'sini yakalamıyordu → üretimde tüm sağlayıcı webhook'ları **419** alırdı; testte CSRF atlandığı için görünmüyordu | `bootstrap/app.php` `preventRequestForgery(except: ['webhooks/*'])`; regresyon testi istisna listesini doğrular | `WebhookCenterTest::gelen_webhook…` | ✅ |

## C–O. Alan raporları (re-audit)

- **Mock / hardcoded / localStorage:** PASS — `MockDataDetectionTest` kilitli; `Http::` Gateway dışı 0; `env()` config dışı 0; TODO/FIXME 0; tarayıcı depolaması 0; JS'ten HTTP 0.
- **Admin ↔ frontend parity:** favicon (F-20), çerez bandı (F-06), yasal sürümler (F-07), defter (F-15) kapandı. Kalan bilinçli boşluk: iyzico/e-Fatura (planlı), tema renk/tipografi paneli (P2).
- **Güvenlik:** CSP nonce (script-src `'unsafe-inline'` yok), TrustProxies, HSTS, upload zorunlu yeniden kodlama, PII şifreleme, anahtar rotasyonu, CSRF istisnası doğru; `composer audit`/`npm audit` 0.
- **Tenant / RBAC:** değişmedi (PASS); `withoutTenantScope` allowlist'e `RetentionService` gerekçeyle eklendi; `ledger.view` artık kullanımda.
- **Entegrasyon / SEO / GEO / AI / Cache:** değişmedi (PASS); planlı sağlayıcılar dürüst işaretli.
- **CI/CD:** kapı + güvenlik + **release** (artefakt, sha256, provenance) + deploy betiği + smoke; dependency audit.
- **Backup:** gerçek prova testte; üretim provası (mysqldump) sunucuda yapılmalı.
- **Gözlemlenebilirlik:** health-alert + doctor satırları (yedek, saat, NTP, failed_jobs, anahtar, KVKK).
- **Test kapsamı:** 370 test; eklenenler: SchedulerIsolationTest, RetentionTest, KeyRotationTest, HealthAlertTest, BackupRestoreTest, SmokeCommandTest + 6 mevcut sınıfa genişletme.

## P. Production gate — kalan koşullar (kod dışı)

```
DO NOT DEPLOY until:
1. Hukuk: MASAK yükümlülük değerlendirmesi (F-10) ve KVKK aydınlatma/çerez metinleri panelden yayınlanıp
   Footer'da seçildi (doctor: "KVKK metni sürümü" ok).
2. Sunucu: TRUSTED_PROXIES, BACKUP_PATH (sunucu dışı, immutable) + BACKUP_ENCRYPTION_KEY (ayrı saklanır),
   chrony/NTP, clamd, Redis, cron, queue:work, mysqldump/pg_dump PATH'te; APP_ENV=production, APP_DEBUG=false.
3. İlk üretim provası: deploy/deploy.sh ile artefakt deploy → doctor 0 hata → smoke 0 hata; ofisvio:backup →
   staging'de ofisvio:restore --force → smoke.
4. Ürün kararı: iyzico/e-Fatura adaptörleri (planlı) — online ödeme olmadan tahsilat manuel.
```

Bunlar sağlandığında kod tarafı için karar: **PASS**. Sağlanmadan "production-ready" denmez.
