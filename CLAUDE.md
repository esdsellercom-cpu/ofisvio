# Ofisvio — çalışma kuralları

Laravel 13 / PHP 8.3. Sanal ofis & coworking SaaS; çok kiracılı (organization → company).
Dil: kod yorumları, commit mesajları ve UI **Türkçe**; tanımlayıcılar İngilizce.

## §76 Kalite kapısı — her değişiklikten sonra dördü de yeşil olmalı

```
./vendor/bin/pint --test
./vendor/bin/phpstan analyse        # level 6, 0 hata
php artisan test                    # Unit + Feature + Architecture
npm run build
```

Kırmızı testi geçirmek için test **gevşetilmez**, kök neden düzeltilir. PHPStan seviyesi
düşürülmez, `@phpstan-ignore` / baseline eklenmez. `tests/Architecture/ArchitectureTest.php`
mimari kuralları kaynak taramasıyla zorlar; allowlist'e ekleme yalnızca gerekçeli yorumla.

## Zincir: auth → account.active → verified → staff.2fa → tenant → permission (bkz. routes/panel.php)

- Personel (global internal rol) doğrulanmış 2FA olmadan `/panel/hesap*` dışında hiçbir ekrana giremez (`EnsureStaffTwoFactor`; `security.require_customer_2fa` açıksa şirket sahibi/yöneticisi de). Panel `verified` ister: davetli şifre belirleyince doğrulanır. Şifre politikası `AppServiceProvider` (`Password::defaults`). Matristeki izin kodda kullanılmıyorsa `rbac_planned_permissions.txt`'de gerekçeli olmalı (ArchitectureTest). Testlerde `staff()` fixture'ı 2FA'lı personel üretir; zorunluluk testleri `staffWithoutTwoFactor()` kullanır. Hesap durumu/oturumlar/giriş geçmişi `AccountSecurityService` (`users.status`, `sessions`, `login_events`); askıdaki hesap girişte genel hata alır, açık oturumu `account.active` düşürür. Giriş olayları `RecordLoginEvent`'e gider — sentetik oturumlar (perf ölçümü) `Auth::setUser/forgetUser` kullanır. `User` `#[Fillable]` dışı alanlar (`status`, `email_verified_at`) yalnız `forceFill`.

- **Yetki route'ta verilir**, controller'da değil: `->middleware('permission:<izin>[,<kapsam>[,<kaynak tipi>,<kaynak parametresi>]]')`.
  `|` alternatif izindir. İzin adları `database/seeders/data/rbac_scope_permission_matrix.csv`'den gelir; CSV tek kaynaktır.
- **Controller'da DB sorgusu yok.** `TenantContext::toArray()` ile doğrulanmış context alınır, servis çağrılır, cevap biçimlendirilir.
- **Servisler HTTP'den bağımsız**: `request()`, `session()` yok (TenantContext ve ContextSwitchService istisna).
- **`companies.status`'a yalnızca `CompanyActivationService` yazar** (state machine + audit).
- **`withoutTenantScope()` her çağrısı güvenlik kararıdır**; ArchitectureTest allowlist'inde gerekçesiyle yer almalı.
- İç içe route'larda `->scopeBindings()` zorunlu; çocuk parametre adı ebeveynin **çoğul ilişki metoduyla** eşleşmeli
  (`{kycDocument}` → `Company::kycDocuments()`, `{userRole}` → `Company::userRoles()`).
- `permission:<izin>,location` organizasyon bağlamı istemez (lokasyon Ofisvio şubesidir); context yalnız `location_id`. `permission:<izin>,anylocation` lokasyonsuz liste ekranı içindir: global YA DA herhangi bir lokasyon grant'i geçer; controller `AuthorizationService::locationIdsWith` (null = hepsi) ile süzer, yabancı lokasyonun kaydı 404 (bkz. `SpaceController`). Lokasyon kapsamlı internal rol (resepsiyon) `user_roles.location_id` ile atanır (`UserAdminService::locationScopedRoles`) ve 2FA zorunluluğuna girer.
- Tenant sınırı ihlali **404** döner (403 kaydın varlığını sızdırır); context yoksa 409 → tarayıcıda seçim ekranı.
- Tenant scope taşımayan modele (Content, Website) tenant rotasından erişim: parametre **int** kalır (model binding yok), servis organizasyona süzer (`ContentService::findForOrganization`), null → 404. Bkz. `SiteController`.
- `Gate::before` yasak ("Super Admin != Root"); JIT izinleri (`requires_jit`) `allows()` ile, rolde-var-mı sorusu `can()` ile.
- Sahte ticari veri yasak: seeder yalnızca referans veri (roller, hizmet kataloğu `services.json`, vitrin blokları `site_blocks.json`). **Şubeler seed edilmez** — `LocationSeeder` yalnız test fixture'ıdır (`DatabaseSeeder` çağırmaz); gerçek şube panelden açılır, vitrin tek/çoklu lokasyon modunu yayındaki şube sayısından seçer. Hizmetler tek kaynak `services` tablosudur; lokasyon ekranı hizmet oluşturmaz, seçer. Kullanıcı/şifre seed edilmez; hesaplar `ofisvio:bootstrap-accounts` (env) ya da `ofisvio:make-admin`.
- **360° üye merkezi** `/panel/uyeler` (faz 51): üye = `user_roles` (şirket üyeliği); profil `member_profiles` (üye no `UYE-…`), sözleşme `contracts` (`SOZ-YYYY-…`, dosya özel `local` diski), ek harcama `extra_charges` — yalnız `MemberCenterService` yazar; finans/tahsis/üyelik/belge şirket bazlı ve mevcut servislerden okunur. Profilden yapılan tahsilat/fatura/üyelik/tahsis mevcut rotalara `return` ile gider (`App\Support\PanelReturn`, yalnız `/panel/` yolu). Görünürlük `CompanyService::visibleTo`; yazma `membership.manage,company` (profil), `subscription.manage,company` (sözleşme), `invoice.issue,company` (ek harcama).
- **Masa/ofis envanteri** `spaces` (saatlik odalar `rooms`), tahsis yalnız `SpaceService::assign/updateAssignment/end` (alan satır kilidi + kapasite; audit); doluluk `SpaceService::occupancy`. Demirbaş `assets` yalnız `AssetService` (tahsise `attach/release`; aynı lokasyon + müsait). Envanter ekranı `/panel/alanlar` tek ekran: okuma `SpaceController::index`, yazma `InventoryController` (modaller `panel.js initModals`: `data-modal-open/data-fill/data-when/data-filter-by`; hata dönüşünde `_modal` gizli alanıyla modal yeniden açılır). Public CSS/JS `asset_v()` ile sürümlenir. Oda/alan/lokasyon bakım durumu `maintenance_until/_note` (`HasMaintenanceStatus`); olanaklar `amenities`; kapak `cover_media_id` yalnız lokasyon galerisinden.
- **Çift rezervasyon savunması** üç katman ve hepsi işlem içinde: `BookingService::lockRoom` (oda satırı) → `assertNoConflict` (tampon dahil, tüm şirketler) → `booking_slots` 15 dk dilim tekil kısıtı (`reserveSlots`; ihlal DomainException). Dilimler yalnız BookingService'te yazılır/silinir; slot süresi 15'in katı. Fiyat: `total_amount` = net (KDV hariç, indirim düşülmüş), `tax_rate/tax_amount`, `discount_amount`, `grandTotal()`; `payment_status` faturadan `syncPaymentStatus` ile.
- **Üyelik/fatura durumu yalnızca servis yazar**: `SubscriptionService` (active/expired/cancelled), `InvoiceService` (draft/issued/overdue/paid/cancelled; tahsilat `recordPayment`, iptal `cancelPayment` — tahsilat silinmez, `payments.status`); fatura iptali JIT'li (`invoice.cancel`). Belgeler (makbuz/geciken ödeme) yalnız `DocumentService` (numara sırası, `data` anlık görüntüsü, iptal); şablon tanımı `App\Documents\DocumentTemplates`, HTML `documents.render` (satır içi CSS — dompdf), PDF `DocumentService::pdf`. Etkinlik kaydı `EventService::register` (kontenjan, KVKK), franchise başvurusu `FranchiseService::apply`. **Tutarlar veritabanında ve serviste kuruş (minor unit) tam sayıdır** — gösterim `money()` / `App\Support\Money::format`, form girdisi (büyük birim, virgül/nokta) `Money::parse`, doğrulama `Money::RULE`, yüzde `Money::percent`; para birimi ayardan (`general.currency`). Tarih sütunlarında `whereDate` (SQLite/MySQL uyumu).
- **Rezervasyon durumu yalnızca `BookingService::transition` yazar** (`BookingStatus` durum makinesi); onay politikası/uygunluk kuralları `SettingsService` ayarlarından. Bildirim: servis doğrudan sağlayıcı çağırmaz — `NotificationService::dispatch` → kuyruk → kanal → `*ProviderInterface` (Gateway). Yeni olay = `NotificationEvents::registry()` kaydı (varsayılan kurallar `seedDefaultRules` ile olay bazlı gelir); müşteri muhatabı `MembershipService::primaryContact`. Zamanlayıcılar `routes/console.php`; hatırlatmalar tek seferlik damga kolonuyla (`due_reminder_sent_at`, `expiring_notice_sent_at`). Alıcı telefon/e-posta yalnız DB (`notification_recipients`); kritik değişiklikler `AuditService::record`.
- **Ticari/CMS içerik config'te olmaz** (fiyat, telefon, e-posta, liste): tek kaynak veritabanı + panel. `config/ofisvio.php` yalnız teknik sabit taşır; `MockDataDetectionTest` bunu zorlar. Tarayıcı depolaması (localStorage vb.) ve JS'ten doğrudan HTTP çağrısı yasak.

- **Ana sayfa görsel/lokasyon/franchise (faz 53):** medya yoksa boş görsel alanı `site.partials.illustration` + `App\Site\Illustrations` (SVG set `public/images/illustrations`, alt metni şehirli) — gerçek `Media` varsa o basılır, `shot__note` yer tutucu metni vitrine dönmez. **Tek lokasyon modu** `SiteBlockService::singleLocation()` (istek memo'su `ContentCache::memo`): yayında tam bir şube → bölge seçimi/liste yok, şube öne çıkar, metin varsayılanları `config('ofisvio.texts_single')` `{city}`/`{city_da}` (`TurkishSuffix::locative`), görünümler `$singleLocation` alır; şube alanı boşsa basılmaz, uydurma yok; tek lokasyon bloğu görsel ağırlıklı (kapak/illüstrasyon, ŞEHİR başlığı, adres/ulaşım (`locations.transport`)/saat/telefon, `Location::directionsUrl()`), ana sayfa JSON-LD'ye `GeoService::localBusinessNode`. Hero: tek lokasyonda Şehir/Bölge alanı yok (`data-hero-location` → teklif formu gizli `location_id`), 2+ şubede seçim (`data-location-ids`); süzgeç → form aktarımı `ofisvio.js initHeroFilter`. Üst şerit sayı metni `texts.topbar_count` (`{count}`, boş = gizli); telefon/WhatsApp/e-posta editörden `globals.contact` ile site ayarına yazılır (yalnız `website.manage`, `SiteBuilderService::applyContact`). Franchise: bölüm tipi `franchise` (`SectionLibrary::FRANCHISE_DEFAULTS`), sayfa `/franchise` (head `SiteLayoutComposer`, JSON-LD sayfada), başvuru numarası `FR-YYYY-000001` yalnız `FranchiseService::apply`, durumlar `FranchiseApplication::STATUSES` (6), bütçe `BUDGETS` seçim listesi (₺ literal kalıbına takılmayan etiketler).
- **Akıllı URL / yönlendirme (faz 54):** yönlendirme kaydı YALNIZ `UrlHistoryService::save/recordMove/recordDeletion` yazar (zincir düzleştirme + döngü reddi + audit orada); içerik/hizmet/lokasyon servisleri slug değişince `recordMove`, silince/arşivleyince `recordDeletion` (anlık görüntüyle) çağırır — yeni URL taşıyan bir varlık eklerken aynı kancalar bağlanır. Karar zinciri `RedirectService::onNotFound` (HTTP'siz; bootstrap NotFoundHttpException render'ı çağırır), benzerlik `App\Seo\RedirectMatcher` (yalnız öneri; eşikler `redirect.*` SEO ayarları). Rastgele 404 asla ana sayfaya yönlendirilmez; geçici (taslak) kaldırılan içerik kalıcı yönlendirme almaz. Silme rotaları önce onay sayfasına gider (`…/sil`, `redirect_mode`). Panel `/panel/seo/{site}/yonlendirmeler` (`RedirectController`), bot `RedirectService::scan`, dış bağlantı yalnız `Gateway::probe`.
- **Canlı düzenleme (faz 59):** vitrinde görsel hedefi `{!! ofv_le(kind, id, field, index, etiket, mediaId, altÖnerisi) !!}` (yalnız `ofv_live()` — yetkili oturum + oturum bayrağı; ziyaretçiye hiçbir şey basılmaz). Yazma yalnız `LiveEditService` (hero / bölüm taslak+yayın / içerik-hizmet-lokasyon kapağı; audit), rotalar `/panel/canli/*` panel zincirinde ve yetki route'ta. JS (`public/js/live-edit.js`) HTTP çağırmaz: anında önizleme + tek form POST. Yeni görsel taşıyan görünüm eklerken `ofv_le` işaretini koy. **Medya adresleri host'a göre bağıldır** (`Media::url()` → `/storage/…`; CSP `img-src 'self'` ve çoklu Host için) — og:image/JSON-LD gibi mutlak adres isteyen yer `Media::absoluteUrl()`/`Media::absolute()` kullanır; `<img>`'e asla APP_URL'lu adres yazma. Yükleme `MediaService::upload` içinde otomatik boyutlandırılır (`MAX_EDGE` 2400 px, EXIF yönü, giriş `MAX_BYTES` 10 MB); form doğrulamaları bu sabitle hizalı. `ofisvio:doctor` medya dosyalarının diskte var olduğunu denetler.
- **SEO & GEO Command Center (faz 60):** sağlık merkezi `App\Seo\HealthCenter` — yeni bulgu türü = `detect()` içine `$add(kategori, kimlik, önem, başlık, url, neden, öneri, fix?)`; otomatik düzeltme = `FIXES` + `autofix()` dalı (mode edit → seo.edit, critical → seo.settings+JIT; iki ayrı rota). Şema çıktısı `App\Seo\SchemaInspector::headFor(website, path)` (composer'ın kullandığı üreticiler; yeni sayfa türü = composer match + inspector dalı) ve `SchemaValidator`. Hizmet GEO cevapları `App\Seo\GeoAnswers` (alan tanımı tek yer), varlık ilişkileri yalnız `EntityGraphService`, hizmet × şehir sayfaları yalnız `LandingPageService` (kalite kapısı `quality()`; toplu üretim ucu YOK), anahtar kelimeler `KeywordService`, bağlantı grafı `LinkGraphService`. Search Console / GA4 / PageSpeed / AI yalnız `App\Integrations\Google\*` ve `App\Integrations\Ai\*` adaptörleriyle Gateway üzerinden; veri günlük özet tablolarına komutla yazılır, panel yalnız saklananı gösterir — **bağlı değilken rakam basılmaz, tahmini/sahte metrik üretilmez**. AI Content Engine `AiContentService`: aşamalar insan tetiklemeli, onay dört göz, AI yayındaki içeriğe dokunmaz (yenileme = çalışma taslağı). Prompt'lar `ai_prompts` sürümlü; varsayılanlar `App\Content\PromptDefaults`.
- **Header & footer (faz 61a):** yayın `websites.header_config/footer_config`, taslak `builder_globals[header|footer]`, sürüm `site_chrome_versions` — YALNIZ `SiteChromeService` yazar (`href()` allowlist, hex renk, medya sahipliği); composer'lar `$header`/`$footer` basar, önizleme/editörde taslak. Yeni menü/kolon alanı = `HEADER_DEFAULTS/FOOTER_DEFAULTS` + normalize + `chrome.blade` + `chrome-editor.js`. Vitrin alanları `data-le-area` ile panel ekranına köprülenir.
- **Entegrasyon merkezi (faz 61b):** tanım tek yer `App\Integrations\IntegrationRegistry` (yeni entegrasyon = kategori + alan şeması + `test` türü); durum/test/kayıt `IntegrationHub`; panel değerleri `IntegrationConfigRepository` (`integration_settings`, `integration_secrets` APP_KEY ile şifreli) — **secret önceliği: panelde girilen değer doluysa o, yoksa env/config** (`SecretStore::provider()` birleşik okur; adaptörler config()'i doğrudan değil SecretStore'u okur). Secret görünüme/loga/audit'e girmez (maske `••••••••••••`, audit yalnız alan adı `changed_fields`). Secret girişi `secrets.manage`, yazma `integrations.manage`, okuma `integrations.view`. SMTP/S3 çalışma zamanı `IntegrationHub::applyRuntime()` (AppServiceProvider boot, tablo yoksa sessiz).
- **Webhook merkezi (faz 61c):** giden olay = `App\Webhooks\WebhookEvents::REGISTRY` satırı + ilgili serviste `WebhookDispatcher::emit(olay, payload)` (kimlik/durum/tutar + iletişim; saklanan kopya `WebhookPayload::redact` ile maskelenir, gönderilen gövde `webhook_deliveries.body` şifreli). Teslimat yalnız `DeliverWebhook` → `Gateway::deliver` (https + genel ana bilgisayar, HMAC `t.gövde`, gövde/yol loglanmaz; IntegrationLog `webhook_out` yalnız ana bilgisayar); yeniden deneme `DeliverWebhook::BACKOFF`, uç bazlı `retry_max`; tekrar gönderim/test yalnız `WebhookService` (audit). Uç secret'ı `encrypted` cast, yalnız üretildiği anda flash ile bir kez gösterilir. Gelen webhook gövde sınırı `WebhookReceiver::MAX_BODY_BYTES`.
- **Performance Command Center (faz 60f):** `RequestProfiler` (web grubu) örnekleme/yavaş sorgu yazar — testte `PERF_SAMPLE_RATE=0` (QueryBudget sayımı); ölçüm isteği bozmaz (terminate + try/catch). Önbellek anahtarı `ContentCache::key` kurulum kimliği + dil taşır; CMS model kaydı `CacheCascadeObserver` ile sürüm atlatır ve `cache_events` izi bırakır — yeni vitrin varlığı eklerken observer listesine ekle. Performans/önbellek izleri 30 gün budanır.
- **Üretim denetimi düzeltmeleri (2026-09-21, AUDIT-2026-09-21.md):** tahsilat `recordPayment` kilitli satırda (lockForUpdate) + referans idempotency + 60 sn çift tık penceresi; her zamanlayıcı görevi `withoutOverlapping()->onOneServer()` (SchedulerIsolationTest); `member_profiles.identity_number` şifreli (audit maskesi PII anahtarlarını da kapsar); **CSP nonce** — satır içi `<script>` yalnız `nonce="{{ csp_nonce() }}"` ile (dış `<script src>` `'self'` ile yüklenir, nonce TAŞIMAZ ve `strict-dynamic` KULLANILMAZ: host allowlist'i kapatıp tüm dış JS'i engellemişti); herkese açık sayfa HTML'inde nonce bulunmaz (istek başına değişir, ETag/304 önbelleğini bozar) — GA4/GTM bootstrap `public/js/analytics.js` `data-ga4/data-gtm` ile; görünümde `on*=` özniteliği YASAK (`data-confirm`, `data-autosubmit`, `data-navigate`, `data-print`; SecurityHeadersTest tarar), yönetici HTML'i `Csp::withNonce`; çerez rızası `App\Site\CookieConsent` (GA4/GTM yalnız `all`); yasal metin sürümleri yalnız `LegalDocumentService` (footer seçimi + içerik yayını tetikler; vitrin rızaları `ConsentRecord`); KVKK saklama yalnız `RetentionService` (`privacy.*` ayarları); append-only defter `LedgerEntry` yalnız `InvoiceService::ledger` (güncelleme/silme model düzeyinde reddedilir); şifreli alan listesi `KeyRotationService::FIELDS` (yeni `encrypted` cast eklerken oraya satır); yedek `BackupService` (`ofisvio:backup/restore`, BackupRestoreTest gerçek prova); deploy `deploy/deploy.sh` + `ofisvio:smoke` (doctor ortam, smoke çalışan sistem); `ofisvio:install` CLI (web /install yok); adaptörü olmayan sağlayıcı `IntegrationRegistry` `kind: planned`; medya yüklemede her görsel GD ile yeniden kodlanır (polyglot/EXIF düşer, GD yoksa ret); gelen webhook CSRF istisnası `bootstrap/app.php` `preventRequestForgery(except: ['webhooks/*'])` (rota düzeyi `withoutMiddleware` L13'te çalışmaz).
- **Marka stili:** `websites.brand_style` yalnız `BrandStyle::normalize` (hex allowlist + sabit font listesi; serbest CSS/URL yok) — vitrin `:root` değişkeni nonce'lu `<style>` ile, Google Fonts sorgusu `BrandStyle::fontFamilies`. Defter düzeltmesi `InvoiceService::correction` (JIT `ledger.correction_entry`, fatura bakiyesine dokunmaz).
- **Denetim (faz 52):** `PanelSmokeTest` her parametresiz panel rotasını 5 rolle açar — yeni sayfa eklerken kırılırsa görünüm/değişken eksiktir; `IdorProbeTest` şirket kapsamlı erişimi, `CriticalFlowsTest` uçtan uca akışları korur. `AuditService::record(null, …)` oturumdaki kullanıcıyı yazar (servis HTTP'den bağımsız kalır). Yakalanmayan `DomainException` bootstrap'ta forma döner. Entegrasyon merkezi `/panel/ayarlar/api` (faz 61b; çekirdek/env ekranı `/panel/ayarlar/api/cekirdek`), Sistem sağlığı `/panel/ayarlar/saglik` (`ofisvio:doctor` tek kaynak). Panel formları gönderimde `aria-busy` (panel.js `initBusyForms`; `data-no-busy` ile kapatılır).

## Görünüm katmanı

- SEO: yalın ayar `websites.seo_*` (+ `/panel/seo`), gelişmiş ayar `websites.seo_settings` JSON — tanım `App\Seo\SeoSettingsRegistry` (yeni ayar = oraya satır; sekme modu izni belirler: edit/critical/integration/entity), okuma `SeoSettingsService::for/get`. Head verisi `SeoService::head` (composer basar), robots/sitemap/llms/HTML site haritası `Site\SeoController`. `SiteSeoPolicy` GLOBAL middleware: Host→Website çözümlemesi + vitrin yönlendirme/başlık politikası (panel/kimlik yolları atlanır; test istemcisi sondaki eğik çizgiyi kırpar — middleware doğrudan çağrılır). Dış istek (IndexNow) yalnız `Integrations\Gateway`.
- Tasarım sistemi `public/css/ofisvio.css` (derlenmez, `<link>` ile). `resources/css/app.css` boş giriş noktası — public asset'i `@import` ETME (Vite build kırılır).
- Ana sayfa bölümleri `SiteBuilderService` (taslak `site_sections` → yayın `site_revisions`); vitrin yalnız yayınlanmış anlık görüntüyü basar, `site.section-wrapper` + `@include('site.sections.<tip>')`. Yeni bölüm tipi = `SectionLibrary` (grup/ikon/alanlar/`defaults`) + `resources/views/site/sections/<tip>.blade.php`; düzenlenebilir metin `{!! ofv($s, 'alan', 'texts.anahtar') !!}`, tekrarlı madde hücresi `{!! ofv_item('alan', $i, $sütun) !!}` (`lines` alanı `columns` tanımlıysa panelde + Ekle / Sil / sürükle-bırak), görsel `data-ofv-image` yalnız `ofv_editor()` ile. `EditorCoverageTest` her bölüm tipinin her alanının çerçevede işaretli olmasını zorlar — yeni alan eklerken görünüme işaret koy. Blog bölümü verisi `ContentService::homePosts` (öne çıkan `is_featured` + en yeni); başlangıç yazıları `ofisvio:blog-starter` (`AppContentBlogStarter` metinleri, `CoverArtist` kapakları) — yeni yazı eklerken aynı SEO/GEO alanlarını doldur, iç bağlantıyı var olan yola ver. **Görsel editör (faz 49)** `/panel/icerik/tasarim`: gerçek vitrin imzalı `?editor=1` çerçevede (`public/js/site-editor.js` üst, `site-editor-frame.js` çerçeve; canlıya editör kodu gitmez), tek gönderimli kayıt `applyDraft` (payload JSON + `uploads[token]`), tasarım allowlist `App\Site\SectionStyle`, globaller `websites.builder_globals` (yayınla → SiteBlockService), kayıtlı bloklar `site_block_presets` (kategori, `is_global`; global blok `site_sections.preset_id` ile bağlı, ayar çizim anında bloktan — editörde düzenlenince bloğa yazılır), sayfa şablonları `App\Site\PageTemplates`. `/panel/icerik/bloklar` yalnız **Blok kütüphanesi** (liste/önizleme/kopya/ad/sil/editöre aç) — düzenleme formu koyma; site metinleri ve veri listeleri (`DATA_BLOCK_SECTIONS`) editörde, taslak→yayın.
- Panel sayfaları `layouts.panel`'i extend eder; `$activeOrganization`, `$isStaff`, `$canSwitchOrganization`, `$panelMenu`, `$uiTheme` `PanelLayoutComposer`'dan gelir (`panel.*` görünümlerine de bağlı). Panel kabuğu `public/css/panel.css` (token eşlemesi + `ap-` bileşenleri; koyu tema `html[data-theme]`), menü `App\View\Menu\PanelMenu` (yeni modül = oraya öge; rozet = `PanelBadgeService` tek sorgu).
- Türkçe metinler `lang/tr/*` ve `lang/tr.json`; rol etiketleri `lang/tr/roles.php`.
- **CMS stüdyo (faz 48):** içerik gövdesi Markdown'dır, HTML `App\Content\BodyRenderer` üretir (bloklar `:::tür`, kısa kodlar, görsel öznitelikleri; ham HTML süzülür) — yeni blok = `BodyRenderer::BLOCKS` + `site/blocks/<tür>.blade.php`. Editör verisi `App\Content\StudioPresenter::build` (SEO analizi `SeoAnalyzer`, GEO önerileri `GeoSuggester` deterministik, kaydedilmez), JS `public/js/cms.js` (`@push('scripts')`). Önizleme imzalı `site.preview.content` iframe'i (`?draft=1` taslağı bellekte bindirir). Yayınla = `store.publish`/`update.publish` rotaları → `ContentService::publishNow`. Medya kırpma/yükleme editörden ayrı formlarla, `return` yalnız `/panel/` yolu.

## Windows notları

PowerShell 5.1: `Get-Content -Raw` ANSI okur — UTF-8 dosyaları `[IO.File]::ReadAllText($f, [Text.Encoding]::UTF8)` ile oku, BOM'suz yaz.
Vendor binary'leri `.\vendor\bin\pint.bat`, `.\vendor\bin\phpstan.bat`.

Yol haritası: `ROADMAP.md`. Yeni modül kalıbı: `routes/BOOTSTRAP.md`. Üretime alma: `DEPLOY.md` + `php artisan ofisvio:doctor` (üretimde hata → çıkış 1). `DatabaseSeeder` yalnız referans veri çağırır; kullanıcı seed'i eklenmez.
